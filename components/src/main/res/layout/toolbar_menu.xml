<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="?attr/actionBarSize"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/ivMenuSearch"
            android:layout_width="@dimen/toolbar_height"
            android:layout_height="match_parent"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="6dp"
            android:src="@drawable/ic_navigation_menu_search"
            tools:ignore="ContentDescription" />

        <ImageView
            android:id="@+id/ivMenuDoctor"
            android:layout_width="@dimen/toolbar_height"
            android:layout_height="match_parent"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="6dp"
            android:src="@drawable/ic_navigation_menu_doctor"
            tools:ignore="ContentDescription" />

        <ImageView
            android:id="@+id/ivMenuAuth"
            android:layout_width="@dimen/toolbar_height"
            android:layout_height="match_parent"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="6dp"
            android:src="@drawable/ic_navigation_menu_auth"
            tools:ignore="ContentDescription" />

    </LinearLayout>

</layout>

